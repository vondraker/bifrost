When implementing changes:

- ALWAYS show a git diff
- NEVER describe changes without diff
- NEVER summarize instead of showing diff
- Diff must include:
  - new files
  - modified files
  - removed code

Format:
```diff
<git diff>
```